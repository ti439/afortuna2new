---
import Modal from '../../components/Modal.astro';


export interface Props {
  tour: {
    nombre: string;
    descripcion?: string;
    descripcionCorta?: string;
    descripcionLarga?: string;
    imagen: string;
    imagenDetalle?: string;
    dificultad: string;
    duracion: string;
    ubicacion: string;
    tags?: string[];
    precio: number;
    queEsperar?: string[];
    incluye?: string[];
  };
}

const { tour } = Astro.props;
const whatsappText = encodeURIComponent(`Hola, quiero reservar el tour ${tour.nombre}`);
const heroImage = tour.imagenDetalle ?? tour.imagen;
const shortDescription = tour.descripcionCorta ?? tour.descripcion;
const longDescription = tour.descripcionLarga ?? tour.descripcion;
---
 
 
 <main class="max-w-5xl mx-auto py-10 px-4 space-y-6">
      <div class="flex items-center">
        <a
          href="/tours"
          class="inline-flex items-center gap-2 text-sm font-medium text-gray-700 hover:text-gray-900 transition"
        >
          <span aria-hidden="true">←</span>
          Ver más tours
        </a>
      </div>
      <div class="flex flex-col md:flex-row gap-6">
        <img src={heroImage} alt={tour.nombre} class="w-full md:w-1/2 rounded-xl object-cover shadow" loading="lazy" />
        <div class="flex-1 space-y-3">
          <h1 class="text-3xl font-bold">{tour.nombre}</h1>
          {shortDescription ? <p class="text-gray-700">{shortDescription}</p> : null}
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm text-gray-600">
            <p><strong>Duración:</strong> {tour.duracion}</p>
            <p><strong>Ubicación:</strong> {tour.ubicacion}</p>
            <p><strong>Dificultad:</strong> {tour.dificultad}</p>
            <p><strong>Precio:</strong> ${tour.precio}</p>
          </div>
          {tour.tags?.length ? (
            <div class="flex flex-wrap gap-2">
              {tour.tags.map((tag) => (
                <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-xs">{tag}</span>
              ))}
            </div>
          ) : null}
          <div class="flex gap-3 pt-2">
            <!-- <button
              onclick="document.getElementById('modal-info').showModal()"
              class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition"
            >
              Ver más detalles
            </button> -->
            <a
              href={`https://wa.me/50663156210?text=${whatsappText}`}
              onclick="return gtagSendEvent(this.href, this.target)"
              target="_blank"
              class="bg-green-100 text-green-800 px-4 py-2 rounded border border-green-300 hover:bg-green-200 transition"
            >
              Reservar vía WhatsApp
            </a>
          </div>
        </div>
      </div>

      {longDescription ? (
        <section class="space-y-3">
          <h2 class="text-2xl font-semibold">Descripción</h2>
          <p class="text-gray-700 leading-relaxed">{longDescription}</p>
        </section>
      ) : null}

      {tour.queEsperar?.length ? (
        <section class="space-y-3">
          <h2 class="text-2xl font-semibold">Qué esperar</h2>
          <ul class="list-disc list-inside space-y-2 text-gray-700">
            {tour.queEsperar.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </section>
      ) : null}

      {tour.incluye?.length ? (
        <section class="space-y-3">
          <h2 class="text-2xl font-semibold">Incluye</h2>
          <ul class="list-disc list-inside space-y-2 text-gray-700">
            {tour.incluye.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </section>
      ) : null}

      <Modal id="modal-info" title="Descripción del Tour">
        <div class="space-y-4 text-sm text-left leading-relaxed">
          <img src={heroImage} alt={`Detalle del tour ${tour.nombre}`} class="rounded-lg mb-4 object-cover w-full" loading="lazy" />
          {longDescription ? <p>{longDescription}</p> : null}
          {tour.incluye?.length ? (
            <div>
              <p class="font-semibold mb-2">Incluye:</p>
              <ul class="list-disc list-inside space-y-1">
                {tour.incluye.map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
            </div>
          ) : null}
        </div>
      </Modal>
</main>
